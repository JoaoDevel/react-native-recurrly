# Copy this file to .env and replace the placeholder with your values.
# Do not commit .env (see .gitignore).

# Clerk Dashboard → API Keys → Publishable key (safe for client apps, but keep .env local)
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here
